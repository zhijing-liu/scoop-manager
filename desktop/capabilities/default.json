{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "最小权限：页面只需要事件监听（SSE 分块由 Rust 通过事件推送）。进程管理、文件打开等能力全部在 Rust 侧完成，不对前端开放。",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
